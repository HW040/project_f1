const:
  default-page-size: 20

file:
  directory: D:/2024-02/download/Project_1

mybatis:
  # MyBatis ?? XML ?? ??
  mapper-locations: classpath:mapper/*.xml
  # MyBatis Type Alias ???
  type-aliases-package: green.project_1.project

spring:
  application:
    name: GreenGramVer2
  datasource:
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
    # log4jdbc? ??? MariaDB ?? ??
    url: jdbc:log4jdbc:mariadb://localhost/Project_1
    username: root
    password: green502
  servlet:
    multipart:
      max-file-size: 15MB
      # ?? ??? ? ?? ?? ?? ??
      max-request-size: 15MB

springdoc:
  swagger-ui:
    # Swagger UI ??
    default-model-expand-depth: 10
    display-request-duration: true
    groups-order: asc
    operations-sorter: method
    path: /swagger
    tags-sorter: alpha
  cache:
    # Swagger UI ?? ????
    disabled: true
